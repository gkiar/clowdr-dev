Run on 02-01-2018 with:

```
docker run -ti -e AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID} -e AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY} -e METADATA_LOCATION=s3://clowdr-storage/tmptasktoday/metadata.json gkiar/clowdr-task:05012018
```
